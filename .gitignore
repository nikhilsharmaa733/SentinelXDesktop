.gradle/
build/
*.iml

# Android Studio writes this on sync (it points at your Android SDK). Machine-
# specific, and this project doesn't use the SDK at all — it's a plain JVM app.
local.properties
.idea/workspace.xml
.idea/caches/
.idea/libraries/
.idea/modules.xml
.kotlin/

# Never commit a real vault or archive. Test fixtures live in src/test/resources
# and must contain synthetic data only.
*.sxv
!src/test/resources/**/*.sxv
